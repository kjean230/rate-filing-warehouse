"""Phase 2 — extraction.

Turns the bytes Phase 1 landed in data/raw/ into typed rows at three grains, with
a ledger that accounts for every document. The gate is zero silent drops; see
pipeline/extract/outcome.py for what that means mechanically.
"""
